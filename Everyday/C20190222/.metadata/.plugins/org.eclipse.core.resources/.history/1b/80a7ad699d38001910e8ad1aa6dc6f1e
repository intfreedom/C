#include<stdio.h>
#include<stdlib.h>

typedef struct snake
{
	int x;
	int y;
	struct snake *next;
}snake;

snake *head,*food;
snake *q;

int main()
{
//	int i = 3;
//	int* ip;
//	ip = &i;
//	char* p;
//	p = "j";
//	printf("%p,%c\n",p,*p);
//	printf("%p,%d\n",ip,*ip);
//	p = "m";
//	printf("%p,%c\n",p,*p);
//	int s[10];
//	int* point = s;
//	printf("%p\n",s);
//	printf("%p\n",s[0]);
//	printf("%p\n",s[1]);

	struct snake snake1;
	printf("%p\n",snake1);
	printf("%p\n",snake1.x);
	printf("%p\n",snake1.next);
	printf("%p\n",snake1.y);



//	snake *tail;
//	tail=(snake*)malloc(sizeof(snake));
//	tail->x=10;
//	tail->y=100;
////	tail->next=NULL;
//	next->=20;
//	next
//	printf("%p\n",tail->next);
//	printf("%d,%d\n",sizeof(tail),sizeof(tail->next));
	return 0;

}


//#include<stdio.h>
//int main(void)
//{
//	int i = 10;
//	int* p = &i;
//	int** q = &p;
//	int*** r = &q;
////*有两个作用：1.1.声明指针变量，int***表示r的基本类型；从这个定义上，int和int*都是一种类型的声明作用；
////1.2.*表示取值，当我们取r的值时，就是q的地址值&q；
////*r是q的地址值，***r表示的是p指向的地址所保存的值，也就是i；
//	printf("p = %p,the value of i = %d\n",p,*p);//int是整型，输出控制符%d;
//	printf("q = %p,the value of p = %p\n",q,*q);//int*是指针类型，输出控制符%；
//	printf("r = %p,the value of q = %p\n",&q,*r);
//
//	return 0;
//}
////***r=**q=*p=i
