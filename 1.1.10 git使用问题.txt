1.1.1
创建git的目录为D:\002Python\Programming Challenge
Linux对应的git项目，/home/intfreedom/.ssh
以下可以让不同的本地库连接同一GitHub远程库，
https://blog.csdn.net/u014028063/article/details/80395510
git add .  #git add .把该目录下的所有文件添加到仓库
git commit -m " comment" 
git push origin master  实现上传
1.1.1.1
把远程库覆盖本地；
git强制覆盖：
?? ?
git fetch --all
?? ?
git reset --hard origin/master
?? ?
git pull

git强制覆盖本地命令（单条执行）：
?? ?
git fetch --all &&?git reset --hard origin/master &&?git pull

1.1.2
做任何提交，都要进入到git的对应修改文件的本地目录？？？
cd /d/002stud   可在git里切换目录

git init

如何解决failed to push some refs to git


error: Cannot pull with rebase: You have unstaged changes.?

error: Additionally, your index contains uncommitted changes.?


原因：如果有未提交的更改，是不能git pull的

解决：?
先执行 git stash?

再执行 git pull --rebase origin master?


最后再执行? git push -u origin maste


1.1.3
新建一个也报错，用以下方法就可以了
git add .  #git add .把该目录下的所有文件添加到仓库
git commit -m "测试" //这时候应该就可以了

git push -u origin master